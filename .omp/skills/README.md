# Project skills

omp discovers `<cwd>/.omp/skills/<name>/SKILL.md`. These ship with the hugo fork so a clone has the same catalog the owner uses. Edit them for your machine; do not invent a new skill format.

`perplexity` means the `web_search` tool (Perplexity OAuth), not a chat model.
