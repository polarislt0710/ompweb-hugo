# Copy to .env.local. Do not commit the real password.
# Required before binding 0.0.0.0 / using npm run dev:lan.
# OMP_WEB_PASSWORD=
# Optional public hostname allowed as a Next.js dev origin (Cloudflare tunnel, etc).
# OMP_WEB_PUBLIC_HOST=
